1. **You**: Con el siguiente texto:

Traducirlo de inglés a castellano
Conservar la segunda forma del plural
Las notas deben tener el formato (te pongo un ejemplo):
!!!note "Nota"
Las capas similares del modelo OSI y del modelo TCP/IP no son completamente equivalentes; aunque tienen similitudes, son dos modelos independientes.
No deben aparecer en el texto referencias del tipo figura x.x o seccion x.x.x, todas estas referencias serán sustituidas por el número de figura o sección que serán números enteros y correlativos
Las figuras a insertar están en la carpeta /img y se insertarán junto con el pie de imagen de la forma (es un ejemplo), donde direccionamientoIP_x, la x es el número de la imagen:
{text-align: justify}
/// figura
Un navegador web en PC1 usa un protocolo de la Capa 7 (HTTPS) para solicitar una página web al servidor web que se ejecuta en SRV1. Las capas 2, 3 y 4 trabajan juntas para entregar el mensaje a la aplicación adecuada en SRV1. La capa 1 es el medio a través del cual ocurre la comunicación.
///
Los fragmentos de código deberán insertarse así:

```
codigo
```